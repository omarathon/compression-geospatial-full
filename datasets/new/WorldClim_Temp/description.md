WorldClim 2.1 Mean Monthly Temperature — January (tavg_01).
30 arc-second resolution (~1 km/pixel). Int16, values in °C × 10 (tenths of degrees),
nodata=-32768. Originally downloaded as Float32 (°C) from WorldClim v2.1; converted to
Int16 via ×10 scaling (0.1°C precision matches WorldClim's stated accuracy).
5 regions: nyc, iowa, himalayas, sahara, amazon (no ocean — land mask applied).
Source: geodata.ucdavis.edu, no authentication, remote crop via /vsizip//vsicurl/.
